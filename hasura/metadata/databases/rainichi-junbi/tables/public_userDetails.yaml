table:
  name: userDetails
  schema: public
object_relationships:
  - name: user
    using:
      foreign_key_constraint_on:
        column: userDetailId
        table:
          name: users
          schema: public
